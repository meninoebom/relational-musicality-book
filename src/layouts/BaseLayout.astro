---
import { ViewTransitions } from 'astro:transitions';
import AudioPlayer from '../components/AudioPlayer.astro';
import '@fontsource-variable/literata/standard.css';
import '@fontsource-variable/literata/standard-italic.css';
import '@fontsource-variable/inter';
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
  audioLoop?: string;
}

const {
  title,
  description = 'Relational Musicality is a technology of remembering. A short book on how Afro-diasporic music and dance restore what civilization splits.',
  audioLoop,
} = Astro.props;

const fullTitle = title === 'Relational Musicality'
  ? title
  : `${title} — Relational Musicality`;
---

<!doctype html>
<html lang="en" data-audio-loop={audioLoop}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <meta name="author" content="Brandon Brown" />
    <title>{fullTitle}</title>
    <ViewTransitions />
  </head>
  <body>
    <slot />
    <AudioPlayer transition:persist="audio-player" />
  </body>
</html>
