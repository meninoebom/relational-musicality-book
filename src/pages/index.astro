---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Technologies of Remembering">
  <main class="cover">
    <div class="cover-content">
      <p class="cover-kicker">A short book by Brandon Brown</p>
      <h1 class="cover-title">Technologies of Remembering</h1>
      <p class="cover-subtitle">
        How Afro-diasporic choreo-musical practices reintegrate
        what sequential cognition pulls apart.
      </p>
      <a href="/01-what-the-hell" class="cover-cta">Begin reading</a>
    </div>
  </main>
</BaseLayout>

<style>
  .cover {
    min-height: 100dvh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: var(--space-xl);
  }

  .cover-content {
    max-width: 600px;
    text-align: center;
  }

  .cover-kicker {
    font-family: var(--font-heading);
    font-size: var(--text-sm);
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--text-muted);
    margin-bottom: var(--space-lg);
  }

  .cover-title {
    font-family: var(--font-heading);
    font-size: var(--text-3xl);
    font-weight: 700;
    color: var(--text-heading);
    line-height: 1.1;
    letter-spacing: -0.03em;
    margin-bottom: var(--space-lg);
  }

  .cover-subtitle {
    font-size: var(--text-lg);
    color: var(--text-muted);
    line-height: 1.5;
    margin-bottom: var(--space-2xl);
  }

  .cover-cta {
    display: inline-block;
    font-family: var(--font-heading);
    font-size: var(--text-sm);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--accent-gold);
    border: 1px solid var(--accent-gold);
    padding: 0.75rem 2rem;
    border-radius: 4px;
    transition: background var(--duration-fast) ease, color var(--duration-fast) ease;
  }

  .cover-cta:hover {
    background: var(--accent-gold);
    color: var(--bg-primary);
    text-decoration: none;
  }

  /* Blur-to-sharp cover animation */
  @media (prefers-reduced-motion: no-preference) {
    .cover-title {
      animation: focus-in 1.2s var(--ease-out) both;
    }

    .cover-kicker {
      animation: cover-fade 0.8s ease both;
      animation-delay: 0.2s;
    }

    .cover-subtitle {
      animation: cover-fade 0.8s ease both;
      animation-delay: 0.6s;
    }

    .cover-cta {
      animation: cover-fade 0.8s ease both;
      animation-delay: 0.9s;
    }
  }

  @keyframes focus-in {
    from {
      opacity: 0;
      filter: blur(8px);
      transform: scale(0.97);
    }
    to {
      opacity: 1;
      filter: blur(0);
      transform: scale(1);
    }
  }

  @keyframes cover-fade {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }
</style>
